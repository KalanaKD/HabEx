function App() {
  return (
    <main className="min-h-screen bg-slate-50 flex items-center justify-center p-6">
      <div className="bg-white rounded-xl shadow p-6 max-w-sm w-full text-center">
        <h1 className="text-2xl font-bold text-slate-800">Habit + Expense Tracker</h1>
        <p className="mt-2 text-slate-500">Scaffold OK — Vite + React + TypeScript + Tailwind</p>
      </div>
    </main>
  )
}

export default App
