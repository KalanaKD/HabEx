# Web build only (Vite exposes VITE_* to the browser). The anon key is a public,
# RLS-restricted key — safe in the client, but keep the real .env out of git.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
